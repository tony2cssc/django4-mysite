[tool.poetry]
name = "django4-mysite"
version = "0.1.0"
description = ""
authors = ["tony2cssc"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
Django = "^4.2.4"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
